# Nacos帮助文档: https://nacos.io/zh-cn/docs/concepts.html
spring:
  application:
    name: micro-order-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
server:
  port: 9000
  servlet:
    context-path: /api
